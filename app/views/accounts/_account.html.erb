<tr>
  <th><%= account.name %></th>
  <th><%= account.surname %></th>
  <th><%= account.amount %></th>
  <th><%= account.currency %></th>
  <th>
    <%= link_to 'Show', account %>
    <%= link_to 'Edit', edit_account_path(account) %> 
    <%= link_to 'Destroy', account_path(account), method: :delete, data: { confirm: 'Are you sure?' } %>
  </th>
</tr>
